name: Workflow B

on: 
  workflow_run:
    workflows: 
      - Workflow A
    types:
      - completed
    branches:
      - master  


jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        name: Checking out repository

      - name: get properties
        id: json_properties
        uses: ActionsTools/read-json-action@main
        with:
          file_path: "credentials.json"
      
      - run: |
          echo ${{steps.json_properties.outputs.branchName}}
      

